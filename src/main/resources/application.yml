spring:
  application:
    name: lamitrack
  datasource:
    # H2 em memória só pra este esqueleto subir sem Postgres rodando; a #42 troca por Postgres real
    url: jdbc:h2:mem:lamitrack;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
